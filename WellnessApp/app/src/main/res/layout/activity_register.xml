<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_page"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="32dp">

        <TextView
            android:id="@+id/btnBack"
            style="@style/TextTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/create_account_title"
            android:textSize="21sp"
            android:textColor="@color/green_primary"
            android:gravity="center_vertical"
            app:drawableStartCompat="@drawable/ic_back"
            app:drawableTint="@color/green_primary"
            android:drawablePadding="10dp" />

        <TextView
            style="@style/TextBody"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:layout_marginBottom="26dp"
            android:text="@string/register_subtitle"
            android:textSize="13sp"
            android:textColor="@color/text_secondary" />

        <TextView style="@style/FieldLabel" android:text="@string/username_label" />
        <EditText
            android:id="@+id/etUsername"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="48dp"
            android:background="@drawable/bg_input"
            android:hint="@string/username_hint"
            android:inputType="text"
            android:autofillHints="username"
            android:layout_marginBottom="12dp" />

        <TextView style="@style/FieldLabel" android:text="@string/email_label" />
        <EditText
            android:id="@+id/etEmail"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="48dp"
            android:background="@drawable/bg_input"
            android:hint="@string/email_hint"
            android:inputType="textEmailAddress"
            android:autofillHints="emailAddress"
            android:layout_marginBottom="12dp" />

        <TextView style="@style/FieldLabel" android:text="@string/password_label" />
        <EditText
            android:id="@+id/etPassword"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="48dp"
            android:background="@drawable/bg_input"
            android:hint="@string/password_dots_hint"
            android:inputType="textPassword"
            android:autofillHints="newPassword"
            android:layout_marginBottom="12dp" />

        <TextView style="@style/FieldLabel" android:text="@string/confirm_password_label" />
        <EditText
            android:id="@+id/etConfirmPassword"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="48dp"
            android:background="@drawable/bg_input"
            android:hint="@string/password_dots_hint"
            android:inputType="textPassword"
            android:autofillHints="newPassword"
            android:layout_marginBottom="20dp" />

        <Button
            android:id="@+id/btnRegister"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:background="@drawable/bg_button_auth"
            android:outlineProvider="background"
            android:elevation="3dp"
            android:fontFamily="sans-serif-medium"
            android:letterSpacing="0.015"
            android:text="@string/register_button"
            android:textColor="@color/white"
            android:textAllCaps="false" />

    </LinearLayout>
</ScrollView>
